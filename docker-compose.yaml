version: "3"
services:
  redis:
    image: redis
    ports:
      - "6379:6379"
  app:
    build: .
    environment:
      REDIS_HOST: "redis"
      REDIS_PORT: 6379
    ports:
      - "80:3000"
    volumes:
    - ./:/app
