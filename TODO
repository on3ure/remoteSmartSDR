interface + backend

Interface
Backend must save al data in redis (simple set vars)
It has to run on a Raspberry PI zero ...
Python + Flask ?

SmartSDR workstation
Fill in IP (check if it is a valid ip cannot be empty)
Fill in TCP port (check if the port is between 1024 and 65535)

Keybow setup
PTT release delay (slider from 100ms till 500ms  (100,200,300,400,500) with 500ms as default) 
-> results in redis integer (integer between 1 and 5)

Offset ... slider in increments (1, 5, 10, 15, 20, 25, 50, 100, 150, 200, 250, 500, 1000) with 250 as default

Redis Vars
offset  (1-1000) (sets tuning increments)
smartSDRip  (ipv4 ip address)
smartSDRport (port between 1024 and  65535)
pttDelay (integer  1,2,3,4,5)
